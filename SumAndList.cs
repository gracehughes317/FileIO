using System;

public class SumAndList
{
	public SumAndList()
	{
        int sum(byte[] file)
        {
            int count = 0;
            for (int i = 0; i < file.ReadLines(file).Length; i++) {
            count += number;
            }
            return count;
        }
	}
}
